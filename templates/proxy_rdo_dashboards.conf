# {{ ansible_managed }}
# use mod_rewrite and not mod_proxy for letsencrypt handling
RewriteEngine  on
# for lets encrypt automation
RewriteCond %{REQUEST_URI} !^/.well-known/acme-challenge/
RewriteRule ^/(.*)         http://127.0.0.1:3030/$1 [L,P]

ProxyPassReverse / http://127.0.0.1:3030/
